# AI Chat Widget + Admin Takeover — мультиклиент + API key на проект (вариант B)

Домен (пример): **loginov.futuguru.com**

Один backend + одна БД обслуживают много клиентов/сайтов через сущность **Project**.
У каждого Project задаются:
- `openai_api_key` (ключ клиента)
- `assistant_id` (ассистент клиента, с его Vector Store/File Search)
- `instructions`
- `allowed_origins` (домены, где разрешен виджет)

---

## 0) Требования
- Ubuntu 22.04/24.04
- Node.js 20+
- Nginx
- PostgreSQL 14+
- SSL (certbot)

---

## 1) Установка на VPS

### 1.1 Пакеты
```bash
sudo apt update
sudo apt install -y nginx git postgresql certbot python3-certbot-nginx
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm i -g pm2
```

### 1.2 PostgreSQL: база и юзер
```bash
sudo -u postgres psql
```
```sql
CREATE USER aiwidget WITH PASSWORD 'STRONG_PASSWORD';
CREATE DATABASE aiwidget OWNER aiwidget;
\q
```

### 1.3 Код
```bash
sudo mkdir -p /var/www
sudo cp -r ./ai-widget /var/www/ai-widget
cd /var/www/ai-widget/server
npm i
```

### 1.4 ENV
```bash
cp .env.example .env
nano .env
```

Заполните:
- DATABASE_URL
- ADMIN_PASSWORD
- JWT_SECRET
- TELEGRAM_BOT_TOKEN (для телеграм-бота уведомлений, опционально)

⚠️ `OPENAI_API_KEY` больше не нужен в .env — ключи задаются по каждому Project в админке.

### 1.5 Схема БД
```bash
psql "postgresql://aiwidget:STRONG_PASSWORD@localhost:5432/aiwidget" -f ./sql/001_init.sql
```

### 1.5.1 Обновление до версии с пользователями и владельцами проектов

Если у вас уже развёрнут проект, примените новый миграционный файл, чтобы добавить таблицу `users` и связь проектов с владельцами:

```bash
psql "postgresql://aiwidget:STRONG_PASSWORD@localhost:5432/aiwidget" -f ./sql/002_users_and_ownership.sql
```

После этого можно входить под администратором (логин `admin`, пароль из `ADMIN_PASSWORD`) и создавать учётные записи пользователей прямо в админке.

### 1.6 Запуск
```bash
npm run start:prod
pm2 start src/index.js --name ai-widget
pm2 save
pm2 startup
```

Проверка:
```bash
pm2 ls
curl -s http://127.0.0.1:3000/health
```

---

## 2) Nginx + SSL

Скопируйте `deploy/nginx-loginov.futuguru.com.conf` в:
`/etc/nginx/sites-available/loginov.futuguru.com`

```bash
sudo ln -s /etc/nginx/sites-available/loginov.futuguru.com /etc/nginx/sites-enabled/loginov.futuguru.com
sudo nginx -t
sudo systemctl reload nginx
sudo certbot --nginx -d loginov.futuguru.com
```

---

## 3) Админка

- https://loginov.futuguru.com/admin/
- Логин: `admin`
- Пароль: `ADMIN_PASSWORD` из `.env`

В проекте (Project) обязательно заполнить:
- OpenAI API key (клиента)
- assistant_id (клиента)
- allowed_origins (домены сайтов клиента)

### Telegram уведомления о новых чатах

Отлично, давай зафиксируем **корректную, рабочую инструкцию для README**, уже **с учётом реальных нюансов ESM, dotenv и твоего проекта**, чтобы ты и любой другой человек больше не ловил эти ошибки.

Ниже — готовый блок, можно **копировать целиком**.

---

## 1.5.2 Обновление до версии с Telegram-уведомлениями

Версия добавляет:

* таблицу кодов привязки Telegram,
* поля для связи проекта с Telegram-чатом,
* Telegram-бота для уведомлений о новых диалогах.

---

### Шаг 1. Применить миграцию базы данных

Перейдите в папку сервера проекта:

```bash
cd /var/www/ai-widget/server
```

Запустите миграцию **от имени пользователя postgres**
(это важно, иначе будут ошибки прав доступа):

```bash
sudo -u postgres psql -d aiwidget -f sql/003_telegram_notifications.sql
```

Если видите `Permission denied`, выполните один раз:

```bash
sudo -u postgres psql -d aiwidget -f <(cat sql/003_telegram_notifications.sql)
```

---

### Шаг 2. Установить зависимости Telegram-бота

В папке сервера выполните:

```bash
cd /var/www/ai-widget/server
npm install node-telegram-bot-api dotenv
```

---

### Шаг 3. Проверить переменные окружения

В файле `.env` **обязательно** должна быть переменная:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCDEF_your_real_bot_token
```

Проверьте, что токен читается корректно:

```bash
node -e "require('dotenv').config({path:'./.env'}); console.log(process.env.TELEGRAM_BOT_TOKEN ? 'TOKEN OK' : 'TOKEN MISSING')"
```

Ожидаемый вывод:

```text
TOKEN OK
```

---

### Шаг 4. Запуск Telegram-бота (важно!)

⚠️ Проект использует **ESM (import/export)**, поэтому `dotenv` должен быть загружен **до импортов**, иначе бот не увидит `.env`.

#### Рекомендуемый способ запуска

Запускайте бота так:

```bash
node --import dotenv/config src/telegram-bot.js
```

Или через npm-script:

```json
"scripts": {
  "bot": "node --import dotenv/config src/telegram-bot.js"
}
```

После этого:

```bash
npm run bot
```

Если всё корректно, вы увидите:

```text
[telegram-bot] Bot started and polling for updates
```

---

### Шаг 5. Запуск бота как сервиса (рекомендуется)

#### Через PM2

```bash
pm2 start src/telegram-bot.js \
  --name ai-widget-bot \
  --node-args="--import dotenv/config"

pm2 save
pm2 startup
```

Проверка:

```bash
pm2 logs ai-widget-bot
```

---

### Шаг 6. Привязка Telegram к проекту

1. Напишите боту в Telegram:

   ```
   /start
   ```

   или

   ```
   /code
   ```
2. Бот выдаст **секретный код**.
3. Откройте админку → **Настройки проекта**.
4. Вставьте код в поле **«Telegram код»** и нажмите **«Привязать»**.
5. После успешной привязки бот начнёт присылать уведомления:

   > «На проекте открыт новый диалог…»

---

### Важно (типовые ошибки)

**Ошибка**

```
[telegram-bot] Telegram bot token is missing
```

**Причина**

* dotenv загружается после ESM-импортов.

**Решение**

* использовать `node --import dotenv/config`
* либо подключать `dotenv` в самом `lib/telegram.js`

---

## Перезапуск Telegram-бота

Способ перезапуска зависит от того, **как именно вы запускаете бота**.

---

### 🔹 Если бот запущен вручную (через `npm run bot`)

1. Остановите бота:

   ```bash
   Ctrl + C
   ```

2. Запустите заново:

   ```bash
   npm run bot
   ```

⚠️ Такой способ **не рекомендуется для продакшена**, потому что бот остановится при:

* закрытии SSH-сессии,
* перезагрузке сервера,
* падении Node.js.

---

### 🔹 Если бот запущен через **PM2** (рекомендуется)

#### Перезапуск бота

```bash
pm2 restart ai-widget-bot
```

#### Остановка бота

```bash
pm2 stop ai-widget-bot
```

#### Запуск бота

```bash
pm2 start ai-widget-bot
```

#### Проверка статуса

```bash
pm2 status
```

#### Просмотр логов бота

```bash
pm2 logs ai-widget-bot
```

или последние 100 строк:

```bash
pm2 logs ai-widget-bot --lines 100
```

---

### 🔹 Если вы меняли `.env` файл

⚠️ **Важно:**
PM2 **не подхватывает изменения `.env` автоматически**.

После любого изменения `.env` (например, `TELEGRAM_BOT_TOKEN`) нужно:

```bash
pm2 restart ai-widget-bot --update-env
```

---

### 🔹 Полный «чистый» перезапуск бота (если что-то пошло не так)

Иногда полезно сделать полный сброс процесса:

```bash
pm2 delete ai-widget-bot
pm2 start src/telegram-bot.js \
  --name ai-widget-bot \
  --node-args="--import dotenv/config"
pm2 save
```

---

### 🔹 Если бот не запускается — быстрая диагностика

1. Проверить, что токен читается:

```bash
node -e "require('dotenv').config({path:'./.env'}); console.log(process.env.TELEGRAM_BOT_TOKEN)"
```

2. Проверить, что бот реально работает:

```bash
pm2 logs ai-widget-bot
```

3. Проверить, что нет второго запущенного бота с тем же токеном
   (Telegram **не допускает два polling-бота одновременно**).

---

### 🧠 Коротко (для README в одну строку)

> После любых изменений в `.env` или коде Telegram-бота выполняйте
> `pm2 restart ai-widget-bot --update-env`

---

## 4) Вставка виджета на сайт клиента

```html
<script
  src="https://loginov.futuguru.com/widget/widget.js"
  data-project-id="PROJECT_UUID_ИЗ_АДМИНКИ"
  data-title="Напишите нам"
  data-position="right"
></script>
```

Если домен сайта клиента не добавлен в allowed_origins — виджет получит 403 origin_not_allowed.

---

## 5) Важный нюанс по правам

`openai_api_key` проекта должен иметь доступ к указанному `assistant_id` (обычно это один и тот же аккаунт/организация).
Иначе OpenAI вернёт ошибку доступа.

---
