diff --git a/widget/widget.css b/widget/widget.css
index e5e423c71d4395a61bf55f4780549c39d12459f7..4405af57b59c7bf37d2094250e1fb54cc61a0559 100644
--- a/widget/widget.css
+++ b/widget/widget.css
@@ -1,34 +1,33 @@
 :root {
-  --aiw-bg: linear-gradient(120deg, #f8f6ff 0%, #f1f5ff 45%, #f8f3ff 100%);
-  --aiw-glow: 0 22px 70px rgba(105, 122, 255, 0.26);
-  --aiw-primary: #6a71ff;
-  --aiw-primary-2: #9a6bff;
-  --aiw-accent: #ff8fb1;
   --aiw-text: #0b1220;
-  --aiw-muted: #64748b;
-  --aiw-panel-radius: 28px;
+  --aiw-muted: #6b7284;
+  --aiw-white: rgba(255, 255, 255, 0.72);
+  --aiw-border: rgba(255, 255, 255, 0.65);
+  --aiw-gradient: radial-gradient(circle at 50% 100%, rgba(255, 165, 205, 0.6), rgba(255, 204, 229, 0) 55%),
+    radial-gradient(circle at 54% 100%, rgba(130, 174, 255, 0.6), rgba(130, 174, 255, 0) 52%),
+    linear-gradient(180deg, #f9f8fb 0%, #f9f8fb 35%, #f9f8fb 100%);
   font-family: 'Inter', 'SF Pro Display', 'Segoe UI', system-ui, -apple-system, sans-serif;
 }
 
 .aiw-fab {
   position: fixed;
   bottom: 24px;
   z-index: 99999;
   width: 64px;
   height: 64px;
   border-radius: 50%;
   border: 0;
   cursor: pointer;
   box-shadow: 0 14px 40px rgba(106, 113, 255, 0.38);
   font-size: 24px;
   line-height: 64px;
   text-align: center;
   background: radial-gradient(circle at 20% 20%, #8fa7ff, #6a71ff 45%, #ff8fb1 100%);
   color: #fff;
   transition: transform 0.3s ease, box-shadow 0.3s ease, opacity 0.35s ease;
   overflow: hidden;
 }
 
 .aiw-fab:hover {
   transform: translateY(-2px) scale(1.05);
   box-shadow: 0 18px 48px rgba(106, 113, 255, 0.45);
@@ -64,257 +63,270 @@
 }
 
 @keyframes aiw-panel-rise {
   0% {
     transform: translateY(40px) scale(0.96);
     opacity: 0;
   }
   60% {
     transform: translateY(0) scale(1.02);
     opacity: 1;
   }
   100% {
     transform: translateY(0) scale(1);
     opacity: 1;
   }
 }
 
 @keyframes aiw-content-fade {
   from { opacity: 0; transform: translateY(10px); }
   to { opacity: 1; transform: translateY(0); }
 }
 
 .aiw-overlay {
   position: fixed;
   inset: 0;
-  background: radial-gradient(circle at 20% 20%, rgba(146, 161, 255, 0.32), transparent 32%),
-    radial-gradient(circle at 80% 10%, rgba(255, 143, 177, 0.35), transparent 34%),
-    linear-gradient(140deg, #f6f7ff 0%, #f2f4ff 50%, #fdf6ff 100%);
-  backdrop-filter: blur(12px);
+  background: var(--aiw-gradient);
   opacity: 0;
   transform: scale(0.98);
   pointer-events: none;
   transition: opacity 0.6s ease, transform 0.6s ease;
   z-index: 99997;
 }
 
 .aiw-overlay.aiw-show {
   opacity: 1;
   transform: scale(1);
   pointer-events: auto;
   animation: aiw-overlay-expand 0.75s ease;
 }
 
 .aiw-overlay-close {
   position: fixed;
   top: 18px;
   right: 18px;
   z-index: 100000;
   border: 0;
   width: 44px;
   height: 44px;
   border-radius: 14px;
-  background: rgba(11, 18, 32, 0.08);
+  background: rgba(255, 255, 255, 0.62);
   color: #0b1220;
   cursor: pointer;
   box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
   opacity: 0;
   transform: translateY(-6px);
   transition: opacity 0.3s ease, transform 0.3s ease;
-  backdrop-filter: blur(6px);
+  backdrop-filter: blur(10px);
 }
 
 .aiw-overlay.aiw-show + .aiw-overlay-close,
 .aiw-overlay-close.aiw-visible {
   opacity: 1;
   transform: translateY(0);
 }
 
 .aiw-panel {
   position: fixed;
   inset: 0;
   display: flex;
   align-items: center;
   justify-content: center;
   z-index: 99998;
   pointer-events: none;
   opacity: 0;
   transform: translateY(20px) scale(0.97);
   transition: opacity 0.35s ease, transform 0.45s cubic-bezier(0.22, 1, 0.36, 1);
 }
 
 .aiw-panel.aiw-open {
   opacity: 1;
   transform: translateY(0) scale(1);
   pointer-events: auto;
   animation: aiw-panel-rise 0.6s ease;
 }
 
-.aiw-card {
-  width: min(1040px, calc(100vw - 48px));
-  height: min(80vh, 640px);
-  background: var(--aiw-bg);
-  border-radius: var(--aiw-panel-radius);
-  box-shadow: var(--aiw-glow);
-  border: 1px solid rgba(106, 113, 255, 0.12);
+.aiw-shell {
+  width: 100%;
+  height: 100%;
   display: flex;
   flex-direction: column;
-  overflow: hidden;
+  align-items: center;
+  justify-content: space-between;
+  padding: 64px 22px 42px;
+  box-sizing: border-box;
 }
 
-.aiw-header {
+.aiw-hero {
   display: flex;
+  flex-direction: column;
   align-items: center;
-  justify-content: space-between;
-  padding: 18px 22px;
-  border-bottom: 1px solid rgba(106, 113, 255, 0.15);
-  background: linear-gradient(120deg, rgba(111, 133, 255, 0.15), rgba(255, 143, 177, 0.08));
-  backdrop-filter: blur(6px);
+  gap: 12px;
+  margin-top: 6px;
+  animation: aiw-content-fade 0.55s ease both;
 }
 
-.aiw-title {
-  font-size: 18px;
-  font-weight: 800;
-  color: var(--aiw-text);
+.aiw-mark {
+  width: 58px;
+  height: 58px;
+  display: grid;
+  place-items: center;
+  background: rgba(255, 255, 255, 0.52);
+  border-radius: 18px;
+  border: 1px solid var(--aiw-border);
+  box-shadow: 0 14px 30px rgba(0, 0, 0, 0.06);
+  backdrop-filter: blur(12px);
 }
 
-.aiw-close {
-  border: 0;
-  background: rgba(11, 18, 32, 0.06);
+.aiw-heading {
+  font-size: 26px;
+  font-weight: 700;
   color: var(--aiw-text);
-  cursor: pointer;
-  font-size: 18px;
-  border-radius: 14px;
-  padding: 8px 10px;
-  transition: background 0.2s ease, transform 0.2s ease;
-}
-
-.aiw-close:hover {
-  background: rgba(11, 18, 32, 0.1);
-  transform: rotate(3deg);
 }
 
 .aiw-body {
+  width: 100%;
   display: flex;
   flex-direction: column;
-  height: calc(100% - 64px);
+  align-items: center;
+  gap: 12px;
   font-size: 16px;
 }
 
+.aiw-chat {
+  width: min(960px, calc(100vw - 48px));
+  display: flex;
+  flex-direction: column;
+  gap: 16px;
+}
+
 .aiw-messages {
-  flex: 1;
-  overflow: auto;
-  padding: 26px 26px 12px 26px;
   display: flex;
   flex-direction: column;
   gap: 16px;
-  background: linear-gradient(180deg, rgba(255, 255, 255, 0.75), rgba(255, 255, 255, 0.92));
+  padding: 20px 20px 16px;
+  background: linear-gradient(140deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.48));
+  border-radius: 26px;
+  border: 1px solid var(--aiw-border);
+  box-shadow: 0 18px 50px rgba(0, 0, 0, 0.08);
+  backdrop-filter: blur(16px);
   animation: aiw-content-fade 0.55s ease 0.1s both;
 }
 
 .aiw-msg {
   display: flex;
 }
 
 .aiw-user {
   justify-content: flex-end;
 }
 
 .aiw-assistant {
   justify-content: flex-start;
 }
 
 .aiw-bubble {
-  max-width: 70ch;
+  max-width: 72ch;
   padding: 12px 14px;
   border-radius: 18px;
   white-space: pre-wrap;
   word-break: break-word;
   font-size: 16px;
   line-height: 1.5;
   box-shadow: 0 12px 30px rgba(15, 23, 42, 0.08);
 }
 
 .aiw-user .aiw-bubble {
-  background: linear-gradient(140deg, #d58cff, #9f8bff 50%, #7db0ff);
+  background: linear-gradient(135deg, rgba(140, 189, 255, 0.78), rgba(158, 132, 255, 0.82));
   color: #f9fbff;
   border-bottom-right-radius: 10px;
 }
 
 .aiw-assistant .aiw-bubble {
-  background: linear-gradient(120deg, rgba(154, 107, 255, 0.12), rgba(125, 176, 255, 0.18));
-  color: var(--aiw-text);
-  border: 1px solid rgba(106, 113, 255, 0.12);
+  background: linear-gradient(135deg, rgba(255, 184, 214, 0.7), rgba(167, 191, 255, 0.7));
+  color: #0c0f1a;
+  border: 1px solid rgba(255, 255, 255, 0.6);
   border-bottom-left-radius: 10px;
 }
 
+
 .aiw-status {
-  padding: 0 26px 10px 26px;
+  padding: 0 4px;
   color: var(--aiw-muted);
   font-size: 13px;
   min-height: 20px;
   font-weight: 600;
   animation: aiw-content-fade 0.45s ease 0.18s both;
 }
 
 .aiw-form {
   display: flex;
+  align-items: center;
   gap: 12px;
-  padding: 16px 20px 18px 20px;
-  border-top: 1px solid rgba(106, 113, 255, 0.12);
-  background: rgba(255, 255, 255, 0.95);
-  backdrop-filter: blur(10px);
+  padding: 12px 14px 12px 18px;
+  border-radius: 20px;
+  background: rgba(255, 255, 255, 0.82);
+  border: 1px solid rgba(255, 255, 255, 0.7);
+  box-shadow: 0 18px 50px rgba(0, 0, 0, 0.08);
+  backdrop-filter: blur(14px);
   animation: aiw-content-fade 0.55s ease 0.2s both;
 }
 
 .aiw-input {
   flex: 1;
-  border-radius: 16px;
-  border: 1px solid rgba(106, 113, 255, 0.2);
-  background: #fff;
+  border-radius: 14px;
+  border: 1px solid rgba(12, 15, 26, 0.04);
+  background: rgba(255, 255, 255, 0.6);
   color: var(--aiw-text);
-  padding: 14px 14px;
-  font-size: 16px;
+  padding: 14px 16px;
+  font-size: 15px;
   outline: none;
-  box-shadow: inset 0 1px 2px rgba(15, 23, 42, 0.08);
+  box-shadow: inset 0 1px 1px rgba(15, 23, 42, 0.08);
 }
 
 .aiw-input::placeholder {
-  color: #9aa5c4;
+  color: #9ba3b5;
 }
 
 .aiw-input:focus {
-  border-color: var(--aiw-primary);
-  box-shadow: 0 0 0 3px rgba(106, 113, 255, 0.18);
+  border-color: rgba(121, 132, 255, 0.6);
+  box-shadow: 0 0 0 3px rgba(121, 132, 255, 0.18);
 }
 
 .aiw-send {
-  border-radius: 16px;
+  border-radius: 14px;
   border: 0;
-  background: linear-gradient(135deg, #6a71ff, #ff8fb1);
+  background: linear-gradient(135deg, #93a2ff, #dca1ff);
   color: #fff;
-  padding: 0 18px;
-  font-weight: 800;
+  width: 44px;
+  height: 44px;
+  display: grid;
+  place-items: center;
   cursor: pointer;
-  box-shadow: 0 14px 30px rgba(106, 113, 255, 0.35);
+  box-shadow: 0 14px 30px rgba(106, 113, 255, 0.28);
   transition: transform 0.2s ease, box-shadow 0.2s ease;
 }
 
+.aiw-send svg {
+  width: 18px;
+  height: 18px;
+}
+
 .aiw-send:hover {
   transform: translateY(-1px);
-  box-shadow: 0 18px 36px rgba(106, 113, 255, 0.42);
+  box-shadow: 0 18px 36px rgba(106, 113, 255, 0.38);
 }
 
 .aiw-send:active {
   transform: translateY(1px);
 }
 
 .aiw-panel .aiw-body,
-.aiw-panel .aiw-header {
+.aiw-panel .aiw-hero {
   opacity: 0;
   transition: opacity 0.35s ease 0.2s;
 }
 
 .aiw-panel.aiw-open .aiw-body,
-.aiw-panel.aiw-open .aiw-header {
+.aiw-panel.aiw-open .aiw-hero {
   opacity: 1;
 }
diff --git a/widget/widget.js b/widget/widget.js
index db331bdfb58573c2dcd2658f21d7762c6a8f59e7..b795bff89331eb9d002ae066af6827f0ce99ea65 100644
--- a/widget/widget.js
+++ b/widget/widget.js
@@ -21,72 +21,83 @@
   let visitorId = localStorage.getItem(visitorKey);
   if (!visitorId) {
     visitorId = (crypto?.randomUUID?.() || String(Math.random()).slice(2) + Date.now());
     localStorage.setItem(visitorKey, visitorId);
   }
 
   let chatId = null;
   let isOpen = false;
 
   // UI
   const btn = document.createElement("button");
   btn.className = "aiw-fab " + (position === "left" ? "aiw-left" : "aiw-right");
   btn.innerHTML = "💬";
 
   const overlay = document.createElement("div");
   overlay.className = "aiw-overlay";
 
   const overlayClose = document.createElement("button");
   overlayClose.className = "aiw-overlay-close";
   overlayClose.setAttribute("aria-label", "Закрыть окно чата");
   overlayClose.textContent = "✕";
 
   const panel = document.createElement("div");
   panel.className = "aiw-panel";
   panel.innerHTML = `
-    <div class="aiw-card">
-      <div class="aiw-header">
-        <div class="aiw-title">${escapeHtml(title)}</div>
-        <button class="aiw-close" title="Закрыть">✕</button>
+    <div class="aiw-shell">
+      <div class="aiw-hero">
+        <div class="aiw-mark" aria-hidden="true">
+          <svg width="26" height="26" viewBox="0 0 26 26" fill="none" xmlns="http://www.w3.org/2000/svg">
+            <path d="M13 0.75C13.4142 0.75 13.75 1.08579 13.75 1.5V4.25C13.75 4.66421 13.4142 5 13 5C12.5858 5 12.25 4.66421 12.25 4.25V1.5C12.25 1.08579 12.5858 0.75 13 0.75Z" fill="#0B1220"/>
+            <path d="M13 21C13.4142 21 13.75 21.3358 13.75 21.75V24.5C13.75 24.9142 13.4142 25.25 13 25.25C12.5858 25.25 12.25 24.9142 12.25 24.5V21.75C12.25 21.3358 12.5858 21 13 21Z" fill="#0B1220"/>
+            <path d="M21 13C21 13.4142 21.3358 13.75 21.75 13.75H24.5C24.9142 13.75 25.25 13.4142 25.25 13C25.25 12.5858 24.9142 12.25 24.5 12.25H21.75C21.3358 12.25 21 12.5858 21 13Z" fill="#0B1220"/>
+            <path d="M0.75 13C0.75 13.4142 1.08579 13.75 1.5 13.75H4.25C4.66421 13.75 5 13.4142 5 13C5 12.5858 4.66421 12.25 4.25 12.25H1.5C1.08579 12.25 0.75 12.5858 0.75 13Z" fill="#0B1220"/>
+            <circle cx="13" cy="13" r="3.25" stroke="#0B1220" stroke-width="1.5"/>
+          </svg>
+        </div>
+        <div class="aiw-heading">Ask our AI anything</div>
       </div>
       <div class="aiw-body">
         <div class="aiw-messages" id="aiw-msgs"></div>
         <div class="aiw-status" id="aiw-status"></div>
         <form class="aiw-form" id="aiw-form">
           <input class="aiw-input" id="aiw-input" placeholder="Ask me anything about your projects" autocomplete="off"/>
-          <button class="aiw-send" type="submit">Send</button>
+          <button class="aiw-send" type="submit" aria-label="Send">
+            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
+              <path d="M3.5 12L20.5 3.5L16 20.5L12.5 13.5L3.5 12Z" stroke="white" stroke-width="1.6" stroke-linejoin="round"/>
+            </svg>
+          </button>
         </form>
       </div>
     </div>
   `;
 
   document.body.appendChild(btn);
   document.body.appendChild(overlay);
   document.body.appendChild(overlayClose);
   document.body.appendChild(panel);
 
-  const closeBtn = panel.querySelector(".aiw-close");
   const form = panel.querySelector("#aiw-form");
   const input = panel.querySelector("#aiw-input");
   const msgs = panel.querySelector("#aiw-msgs");
   const statusEl = panel.querySelector("#aiw-status");
 
   function setStatus(text) {
     statusEl.textContent = text || "";
   }
 
   function append(role, text) {
     const item = document.createElement("div");
     item.className = "aiw-msg aiw-" + role;
     item.innerHTML = `<div class="aiw-bubble">${escapeHtml(text)}</div>`;
     msgs.appendChild(item);
     msgs.scrollTop = msgs.scrollHeight;
     return item;
   }
 
   async function ensureChat() {
     if (chatId) return chatId;
     const r = await fetch(`${BASE}/api/widget/${projectId}/chat/start`, {
       method: "POST",
       headers: { "Content-Type": "application/json" },
       body: JSON.stringify({ visitorId }),
     });
@@ -151,50 +162,48 @@
     panel.classList.add("aiw-open");
     try {
       await ensureChat();
     } catch (e) {
       console.error(e);
     }
     setTimeout(() => input.focus(), 120);
   }
 
   function closePanel() {
     isOpen = false;
     overlay.classList.remove("aiw-show");
     overlayClose.classList.remove("aiw-visible");
     panel.classList.remove("aiw-open");
     setTimeout(() => btn.classList.remove("aiw-hide"), 200);
   }
 
   btn.addEventListener("click", async () => {
     if (isOpen) {
       closePanel();
     } else {
       await openPanel();
     }
   });
 
-  closeBtn.addEventListener("click", closePanel);
   overlayClose.addEventListener("click", closePanel);
-  overlay.addEventListener("click", closePanel);
 
   form.addEventListener("submit", async (e) => {
     e.preventDefault();
     const text = String(input.value || "").trim();
     if (!text) return;
     try {
       await sendMessage(text);
     } catch (err) {
       console.error(err);
       setStatus("Не удалось отправить сообщение");
     }
   });
 
   function escapeHtml(s) {
     return String(s)
       .replaceAll("&", "&amp;")
       .replaceAll("<", "&lt;")
       .replaceAll(">", "&gt;")
       .replaceAll('"', "&quot;")
       .replaceAll("'", "&#039;");
   }
 })();
